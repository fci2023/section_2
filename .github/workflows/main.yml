on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: Ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "Build docker image"
        run: dockhjnyfnhger build -t grig .
